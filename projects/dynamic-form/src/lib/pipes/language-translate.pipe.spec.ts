import { LanguageTranslatePipe } from './language-translate.pipe';

describe('LanguageTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
