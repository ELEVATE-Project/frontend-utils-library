import { Routes } from '@angular/router';
import { SlFormComponent } from './sl-form/sl-form.component';

export const routes: Routes = [
    {
        "path": "forms",
        "component": SlFormComponent
    },
];
